# Rspamd updates module - keeps detection rules up to date
enabled = true;

# Check for updates every 24 hours
update_interval = 1d;

# Enable AV signatures update if available
enable_av = true;

# Enable URL lists update
enable_urls = true;

# Enable module and statistics updates
enable_modules = true;

# Automatically apply updates
auto_update = true;
