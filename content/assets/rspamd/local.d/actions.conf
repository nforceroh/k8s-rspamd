# Action thresholds configuration
# These define the score thresholds for various actions

# Reject mail with high spam score
reject = 15;

# Add spam header for moderate scores
add_header = 5;

# Rewrite subject for high scores
rewrite_subject = 8;

# Apply greylisting for low-moderate scores
greylist = 4;

# Soft reject (temporary failure)
# soft_reject = 12;
