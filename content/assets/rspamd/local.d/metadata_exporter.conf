# /etc/rspamd/local.d/metadata_exporter.conf

rules {
  MAILGALLOWS_INGEST {
    backend = "http";
    url = "http://<YOUR_MAILGALLOWS_SERVICE_IP>:5000/quarantine";
    selector = "is_reject";
    formatter = "default";
    
    # --- HOTFIX: Replaces the deprecated 'meta_headers = true' option ---
    format = "multipart";
  }
}
